import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    buttonBackMenu: {
        marginTop: 30,
    },
    textBackMenu: {
        color: '#fff',
        fontSize: 20,
        fontFamily: 'Montserrat-Bold',
    }
});
