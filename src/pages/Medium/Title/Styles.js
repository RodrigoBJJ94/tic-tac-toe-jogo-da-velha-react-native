import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    title: {
        fontSize: 30,
        fontFamily: 'Montserrat-ExtraBold',
        color: '#fff',
        marginBottom: 70,
    }
});
