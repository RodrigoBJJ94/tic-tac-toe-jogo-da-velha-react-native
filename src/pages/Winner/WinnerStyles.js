import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    winner: {
        fontSize: 30,
        fontWeight: 'bold',
        color: '#000',
        marginBottom: 340,
    },
});
